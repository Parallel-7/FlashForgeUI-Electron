const CameraManager = require('./CameraManager');
const CameraService = require('./CameraService');
const SimpleCameraProxy = require('./SimpleCameraProxy');

// Export camera management components
module.exports = {
  CameraManager,
  CameraService,
  SimpleCameraProxy
};
